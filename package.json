{
  "name": "react-find-rep",
  "version": "0.0.1",
  "private": true,
  "homepage": "https://geoffdavis.info/react-find-rep",
  "devDependencies": {
    "react-scripts": "0.9.0"
  },
  "dependencies": {
    "ajax.js": "^2.0.0",
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "clean": "rm -r build docs",
    "build": "rm -r docs; react-scripts build; mv build docs; rm -r build",
    "_build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
